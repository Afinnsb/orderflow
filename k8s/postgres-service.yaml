apiVersion: v1
kind: Service
metadata:
  name: orderflow-postgres
spec:
  selector:
    app: orderflow-postgres

  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432

  type: ClusterIP